// Copy to auth/secrets.js and paste your OAuth App Client Secret from GitHub.
// GitHub → Settings → Developer settings → OAuth Apps → your app → Client secrets
window.GITHUB_AUTH_SECRETS = {
  clientSecret: "",
  /** Personal Access Token with gist scope — server-side only (auth/secrets.js, gitignored) */
  githubPat: "",
};
